UOM Souvenir Website — Project Context
======================================

Overview
--------
An e-commerce website for selling University of Mauritius (UOM) souvenirs and
merchandise — campus apparel, premium souvenirs, and university-branded
products. The goal is a professional, trustworthy, institutional-feeling
storefront with a product showcase, shopping cart, and (eventually) a full
purchase flow. Live at https://uom-souvenir-website.vercel.app.

Tech Stack
----------
Framework & Language
- Next.js 16.2.7 (App Router) — note: this is a newer Next.js with breaking
  changes vs. older versions
- React 19.2.4 (with React Compiler / babel-plugin-react-compiler enabled)
- TypeScript 5

Styling & UI
- Tailwind CSS v4 (via @tailwindcss/postcss) — utility classes only, minimal
  custom CSS
- GSAP 3 — scroll animations & page transitions
- Lucide React — SVG icons (no emojis as icons)
- Google Fonts: Geist (sans), Geist Mono, Playfair Display (serif display)
- Design direction: "Soft UI" — soft shadows, subtle depth, institutional blue
  palette (base bg #B0E0F8 pale blue, text #1E2019). Single fixed light scheme:
  no dark mode — globals.css sets color-scheme:light to force it for everyone.

Data & Backend
- Neon — serverless Postgres (@neondatabase/serverless, connection pooling)
- Drizzle ORM (drizzle-orm + drizzle-kit) — schema-first
- State: React Context (CartContext) for the cart

Tooling & Deployment
- Vercel (hosting + env vars for Neon connection string)
- Playwright (testing)
- tsx, dotenv

Current Structure
-----------------
- Routes: / (home), /shop, /shop/[id] (product detail), /shop/receipt/[id]
  (reservation receipt), /cart
- Home components: Hero, BestSellers, BestSellersCard, StepsSection,
  FaqAccordion, CategoryCard
- Shop: ShopContent (client-side filter/sort/search) renders ProductCard;
  product detail uses ProductGallery + ProductBuyPanel; CartSummary is shared.
  All product data is DB-backed via src/db/queries.ts — NOT static anymore.
- Cart: CartContext (localStorage-persisted) + /cart page. Checkout calls the
  submitReservation server action (src/app/cart/actions.ts -> src/db/mutations.ts),
  which creates a reservation inside a row-locked transaction (src/db/tx.ts),
  then redirects to the receipt page.
- Layout: Navbar, Footer
- src/data/store-data.ts: now ONLY home-page static content (navLinks,
  categories, bestSellers, workflowSteps, footerLinks) plus the ShopProduct type
  used by the cart — it is no longer the product catalog.
- Colors: the palette lives as hex exports in src/constants/variables.tsx,
  consumed via inline styles / color-mix(). There is NO Tailwind @theme block,
  so bare utilities like bg-pale-blue / text-red generate nothing — don't use
  them (use inline token styles, or an inline CSS var + hover:bg-[var(--x)]).

Conventions & Constraints (important for advice)
------------------------------------------------
- Server Components by default; "use client" only when needed
- Always use Next.js <Image> (never raw <img>) and <Link> for internal nav
- Tailwind utilities only — no inline styles / CSS modules unless forced
- This Next.js version differs from training data — APIs may have changed;
  verify against current Next 16 docs
- "Lazy senior dev" philosophy: prefer the simplest solution, avoid unnecessary
  dependencies/abstractions
- Accessibility: 4.5:1 contrast, visible focus states, prefers-reduced-motion,
  responsive at 375/768/1024/1440px

Dead Code / To Be Deleted
-------------------------
Code-level dead code was removed in commit d3bd2f0 (see Last Updated):
- src/components/reactbits/PillNav.jsx + PillNav.css — DELETED (dir removed too)
- src/data/store-data.ts — DELETED the unused featuredProducts, features,
  ProductItem, FeatureItem and TestimonialItem exports
- Misc dead lines: legacy :root color vars in globals.css, an unused React
  import, an unused RECEIPT_INK const in the receipt page

CORRECTION: the old claim that src/db/index.ts was dead was WRONG — queries.ts
imports its `db` instance and the whole shop/cart/receipt flow is DB-backed.

Still pending (image assets — confirmed unused, not yet removed):
- src/app/images/hero/hero-background-blurred.png
- src/app/images/hero/ogbackgroundpic.jpg
- src/app/images/hero/previous-background.png
- src/app/images/gifs/{book,graduationhat,pen}.gif  (note: gifs dir is gitignored)
(herobackground.jpg IS used by Hero.tsx — keep it.)

Planned next steps
------------------
- Image storage: Cloudflare R2 (S3-compatible object bucket) — decided/in use;
  product images not yet wired in code (shop still uses placehold.co URLs)
- Authentication: better-auth — the reservation flow currently attributes every
  order to a seeded guest user; signed-in users replace the guest once this lands
- Users (including admins / roles) — schema not yet designed
- Order lifecycle beyond reservation (confirm / collect / cancel status changes,
  admin tooling). No online payment — payment is handled offline / out of scope.

Current gaps
------------
- No auth yet — reservations are tied to a seeded guest user
- No admin/role schema or tooling
- Product images are still placehold.co placeholders (R2 not wired in code)
- No CI/test coverage to speak of beyond Playwright being installed

Last Updated
------------
- Date: 2026-06-30
- Commit: d3bd2f0 "project cleanup"
- Full project audit (dead code + color-token compliance). Deleted the unused
  PillNav reactbits files and the unused store-data exports (featuredProducts,
  features, ProductItem, FeatureItem, TestimonialItem); removed dead lines
  (legacy :root vars in globals.css, an unused React import, an unused
  RECEIPT_INK const). Color pass: routed hardcoded/arbitrary colors through the
  variables.tsx tokens (creamHex bg, blackHex/whiteHex for scrims/borders/hover
  tints, redHex/paleBlueHex for the previously-broken text-red / bg-pale-blue
  utilities) using inline styles + inline CSS-var hover pattern. Discovered there
  is no @theme block, so those bare utilities were dead — corrected the misleading
  comments in variables.tsx/globals.css. Left #5b6b86 muted slate hardcoded (by
  decision) and the receipt template colors as their documented locked exception.
  typecheck + build both pass.
